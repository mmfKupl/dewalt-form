// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebase: {
    apiKey: 'AIzaSyAtkSmf8rtF-PdEM1jkTyyQeBMivBIepq4',
    authDomain: 'dewalt-up18-data.firebaseapp.com',
    databaseURL: 'https://dewalt-up18-data.firebaseio.com',
    projectId: 'dewalt-up18-data',
    storageBucket: 'gs://dewalt-up18-data.appspot.com/',
    messagingSenderId: '537001885799',
    appId: '1:537001885799:web:ea2a9175dff2fa31b214c2',
    measurementId: 'G-PG9ML38XMV'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
